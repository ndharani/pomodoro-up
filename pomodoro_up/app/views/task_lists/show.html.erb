<div class="container text-center">

    <h1><%= @task_list.name %></h1>

    <!-- task list metadata -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6">
                Status: <%= @task_list.status %>
            </div>
            <div class="col-md-6">
                List created at: <%= @task_list.created_at.strftime("%m/%d/%Y %H:%M") %>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                Pomodoros started: <%= @task_list.pomodoros_started %>
            </div>
            <div class="col-md-6">
                Pomodoros completed: <%= @task_list.pomodoros_completed %>
            </div>
        </div>
    </div>

    <hr/>
    <br/><br/><br/>

    <!-- tasks -->
    <!-- TODO: remove status from tasks table -->
    <table class="table text-left">
        <thead>
            <tr>
                <th class="col-md">Task</th>
                <th class="col-md"></th>
                <th class="col-md">Status</th>
            </tr>
        </thead>

        <tbody>
            <!-- add tasks -->
            <tr>
                <%= form_for [@task_list, @new_task] do |f| %>
                    <% f.options[:html][:role] = 'form' %>
                    <td><%= f.text_field :name, class: "form-control", placeholder: "Add a new task" %></td>
                    <td><%= f.submit class: "btn btn btn-primary" %><td>
                    <td></td>
                <% end %>
            </tr>
            <!-- list tasks -->
            <% @active_tasks.each do |task| %>
                <tr>
                    <td><input class="checkbox" type="checkbox"> <%= task.name %></td>
                    <td><%= link_to "Delete", [@task_list, task],  method: :delete, data: { confirm: "Are you sure?" } %></td>
                    <td><%= task.status %></td>
                </tr>
            <% end %>
        </tbody>
    </table>

</div>
